# Cloudflare Pages 安全標頭配置
# 文檔: https://developers.cloudflare.com/pages/platform/headers/

# 全域安全標頭
/*
  # 防止點擊劫持
  X-Frame-Options: DENY

  # 防止 MIME 類型嗅探
  X-Content-Type-Options: nosniff

  # 啟用瀏覽器 XSS 過濾器
  X-XSS-Protection: 1; mode=block

  # 推薦來源政策
  Referrer-Policy: strict-origin-when-cross-origin

  # 權限政策（限制瀏覽器功能）
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=(), accelerometer=(), gyroscope=(), magnetometer=()

  # HSTS（強制 HTTPS，有效期 1 年）
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

  # Content Security Policy
  # 注意：因使用 Tailwind CDN 和 Material Web Components，需要 'unsafe-inline'
  # 理想情況下應該使用 npm 安裝並自行託管，或使用 nonce/hash
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.tailwindcss.com https://esm.run; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://www.googleapis.com https://lazy-pagespeed-api.blackflash.workers.dev; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests

# 快取控制（靜態資源）
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/js/*
  Cache-Control: public, max-age=31536000, immutable

# HTML 不快取（確保總是取得最新版本）
/*.html
  Cache-Control: public, max-age=0, must-revalidate
